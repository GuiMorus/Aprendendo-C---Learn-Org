// Importando biblioteca padrão de entrada e saída
#include <stdio.h>

int main(){
    // Iniciando array
    int numeros[5];

    // Atribuindo valores ao array
    numeros[0] = 1;
    numeros[1] = 2;
    numeros[2] = 5;
    numeros[3] = 7;
    numeros[4] = 10;

    return 0;

}